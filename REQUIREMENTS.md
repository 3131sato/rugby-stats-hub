Exit code: 0
Wall time: 0.5 seconds
Output:
# Rugby Stats Hub 要件定義書

## 目的
手入力しているラグビーの選手・チームスタッツを一元管理し、比較とスプレッドシート出力を簡単にする。

## 想定ユーザー
監督、コーチ、アナリスト、マネージャー、記録担当者、選手。

## MVP機能
1. 選手名・所属・ポジションと、指定された基本・攻撃・守備・キック項目を入力・保存する。
2. チーム名と、攻撃・守備・キック・セットプレー項目を入力・保存する。
3. 最大3選手または3チームを横並びにし、項目別最高値を表示する。
4. 全データをUTF-8 BOM付きCSVへ出力し、Excel／Googleスプレッドシートで利用できる。
5. 登録データの一覧表示・削除・ブラウザ自動保存を行う。

## 非機能要件
- PC、タブレット、スマートフォン対応
- 赤・白を基調とした高コントラストUI
- 主要操作は2クリック以内
- GitHubでソース管理し、Vercelでホスティング

## 保存方針
MVPはアカウント不要で即利用できるLocalStorage方式。同じ端末・ブラウザでのみ参照できるため、CSVをバックアップに利用する。

## 将来拡張
試合・シーズン単位の集計、CSVインポート、XLSX出力、ログインとクラウド同期、権限管理、推移グラフ、公式API連携。

## 受け入れ条件
- 全指定項目を入力・保存できる
- 保存後、比較画面に反映される
- 最大3件を比較できる
- CSVがExcelまたはGoogleスプレッドシートで開ける
- 再読み込み後も同一端末でデータが残る
- モバイル幅でも主要操作ができる

